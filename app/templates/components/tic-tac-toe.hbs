{{#if playing}}
  {{#if winner}}
    <div>
      Player {{winner}} Won!
    </div>
  {{/if}}
  {{#if draw}}
      We'll call it a draw.
  {{/if}}
  <button class="absolute" {{action 'start'}}>Restart</button>
{{else}}
  <button class="absolute" {{action 'start'}}>Start</button>
{{/if}}
<canvas id="stage" width="380" height="380"></canvas>
